public class ZooKeeper {
    static void loadFood(Cage firstCage) {
        firstCage.getFood("water");
        firstCage.getFood("hay");
        firstCage.getFood("meat");
        firstCage.getFood("milk");
        firstCage.getFood("seed");
        firstCage.getFood("maggots");
        firstCage.getFood("insects");
    }
}
